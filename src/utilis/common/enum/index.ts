export enum SYS_ROLE {
    admin,
    user
}

export enum GENDER {
    MALE = "male",
    FEMALE = "female"

}

export enum USER_AGENT {
    local,
    google,

}

export enum REACTION {
    LIKE,
    CARE,
    LOVE,
    ANGRY
}

export enum FriendRequestStatus {
    pending = "pending",
    accepted = "accepted",
    rejected = "rejected"
}